apiVersion: v1
kind: Service
metadata:
  name: product-service
spec:
  type: ClusterIP # Internal service
  selector:
    app: product-service
  ports:
    - protocol: TCP
      port: 5002
      targetPort: 5002
